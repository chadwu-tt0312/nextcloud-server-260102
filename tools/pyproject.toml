[project]
name = "nextcloud-tools"
version = "1.0.0"
description = "Nextcloud / MinIO 批次管理 CLI 工具"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
