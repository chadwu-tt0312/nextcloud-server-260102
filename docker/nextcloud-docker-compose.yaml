version: "3.7"
services:
  nextclouddb:
    image: mariadb:10.6
    restart: always
    command: --transaction-isolation=READ-COMMITTED --log-bin=binlog --binlog-format=ROW
    ports:
      - "3306:3306" # 加入這行，將主機 3306 映射到容器 3306
    environment:
      - MYSQL_ROOT_PASSWORD=upspi
      - MYSQL_PASSWORD=upspi
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
    volumes:
      - db:/var/lib/mysql
    networks:
      - n8n-network

  redis:
    image: redis:7
    restart: always
    # volumes:
    #   - ./redis:/data
    networks:
      - n8n-network

  nextcloud:
    image: nextcloud
    restart: always
    ports:
      - 8085:80
    depends_on:
      - redis
      - nextclouddb
    volumes:
      - nextcloud:/var/www/html
      # below mapping is for using External Storage (local) to mount samba shared folder into Nextcloud. Change as needed.
      # - /home/upspi/Desktop/share/nextcloud_share:/mnt/samba
    environment:
      - MYSQL_PASSWORD=upspi
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_HOST=nextclouddb
      - NEXTCLOUD_ADMIN_USER=admin
      - NEXTCLOUD_ADMIN_PASSWORD=password123
    networks:
      - n8n-network

volumes:
  nextcloud:
  db:

networks:
  n8n-network:
    external: true # 這代表網路已經透過 'docker network create n8n-network' 建立過了
